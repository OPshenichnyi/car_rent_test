export const selectIsCarsCatalog = (state) => state.auto.carsCatalog;

export const selectIsPageCatalog = (state) => state.auto.page;

export const selectIsLoad = (state) => state.auto.isLoad;

export const selectIsStatusError = (state) => state.auto.statusError;
